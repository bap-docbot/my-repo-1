name: Just test

on:
  push:
    branches:
    - master

jobs:
  my-job-name:
     name: Test
     runs-on: [self-hosted, ubuntu-latest]
     steps:
       - name: My first step
         uses: ./.github/actions/my-action
