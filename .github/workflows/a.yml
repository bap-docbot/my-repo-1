name: Just test

on:
  push:
    branches:
    - master

jobs:
  my-job-name:
     name: A new job
     runs-on: ubuntu-latest
     steps:
       - name: checkout
         uses: actions/checkout@v2
       - name: my-action
         run: .github/docs/update.sh
